# Trendz API Server

1. Run Yarn
`yarn`
2. Setup database (remember cd server)
`mongorestore --db testTrendz .\dump\trendz-network\`
3. Run server
`yarn develop`
4. Login into strapi:
`username: admin`
`password: Password`